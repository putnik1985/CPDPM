###polynome degree
n = ARGV[1] 


Init(n,x) = (n == 1) ? x : 1
P(n,x) = (n>1) ? (2. * n - 1.)/n * x * P(n-1,x) - (n - 1.)/n * P(n-2,x) : Init(n,x)
 
set xrange [-1.:1.]
###set yrange [-1.:1.]
set grid
unset key
set title sprintf("Legandre Polynom degree = %d\n", n)
plot P(n,x) with lines 
